// u8
const impl FromPatch<u8> for i16 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for i32 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for i64 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for i128 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for isize {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for u16 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for u32 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for u64 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for u128 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for usize {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for f32 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for f64 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for f16 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
const impl FromPatch<u8> for f128 {
    fn from_value(v: u8) -> Self {
        v as Self
    }
}
use crate::FromPatch;
